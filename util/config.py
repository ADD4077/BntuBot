from os.path import abspath

server_db_path = abspath(r"databases\server.db")
states_db_path = abspath(r"databases\states.db")
literature_per_faculty_json_path = abspath(r"books\literature_per_faculty.json")
